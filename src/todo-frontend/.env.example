# Optional: Override the API base URL.
# By default, the Vite dev server proxies /api to http://localhost:5079 (see vite.config.ts).
# Only set this if you need to bypass the proxy (e.g., connecting to a remote API).
# VITE_API_URL=http://localhost:5079/api
